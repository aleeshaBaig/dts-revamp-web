<!DOCTYPE html>
<html lang="en">

<meta http-equiv="content-type" content="text/html;charset=UTF-8" />
<head>

<style type="text/css">
.customizer2 .handle2 {
    position: absolute;
    display: flex;
    top: 300px;
    left: 208px;
    cursor: pointer;
    padding: 10px 8px;
    border-top-right-radius: 4px;
    border-bottom-right-radius: 4px;
    box-shadow: -3px 0 4px rgba(0,0,0,.06);
    font-size: 15px;
    transition: all .3s ease-in-out;
    white-space: nowrap;
    height: 43px;
    transform: rotate(-90deg);
}

.customizer2 .handle2, .customizer2 .handle2:hover {
    background: #135591;
    color: #fff;
    left: 12px;
}

.admin-icon{
  margin-right: 16px;
}

</style>
 
 </head>
<body class="text-left">
  <div class="app-admin-wrap layout-horizontal-bar clearfix">
    <div class="main-content-wrap  d-flex flex-column">
      <div class="main-content"> 

          <!-- Filters Partial -->
          <%= render :partial => 'labs/filters/line_listing_filter', :locals => {} %>

          <div class="row m-t-15">
            <div class="col-md-12">
              <div class="card ">
                <div class="card-body p-0">
                            <h5 class="card-title p-custom1">
                                Labs
                               <!--  <button class="btn btn-success float-right filter-btn2" formaction="<#%patients_path(request.parameters.merge({:format => :xls}))%>">Export to Excel</button> -->

                                
                                  <%= link_to 'Export to Excel', labs_path(format: :xls), :class => 'btn btn-success float-right filter-btn2', :title => "Export to Excel"%>
                               
                            </h5>

                            <div class="separator-breadcrumb border-top mb-3"></div>
                            <div class="listing-tbl-area">
                              <table class="table table-bordered table-hover listing-tbl">
                                <thead>
                                  <tr>
                                    <th>Name</th>
                                    <th>District</th>
                                    <th>Type</th>
                                    <th colspan="3">Action</th>
                                  </tr>
                                </thead>

                                <tbody>
                                  <% @labs.each do |lab| %>
                                  <tr>
                                    <td class="tbl-td-color2"><%= lab.lab_name %></td>
                                    <td class="tbl-td-color2"><%= lab.district.district_name %></td>
                                    <td class="tbl-td-color2"><%= lab.lab_type %></td>
                                    <td class="tbl-td-color2"><%= link_to "Edit".html_safe, edit_lab_path(lab), class: 'edit' %> </td>
                                  </tr>
                                  <% end %>
                                </tbody>
                              </table>
                            </div>
                        </div>
                    </div>
                </div>

            </div>

            


</div>



    <div class="text-center">
      <% if @labs.present? %> 
              <%= will_paginate @labs, :class => 'apple_pagination'  %>
              <div class="apple_pagination"><b style="font-weight: bold;">Total Records: </b><%= @labs.total_entries %></div> 
             <% else %> 
               <div class="apple_pagination"><b style="font-weight: bold;">Total Records: </b> 0 </div> 
       <% end %>
    </div>
    </body>

</html>


